# One-minute submission recording

Record only after a real Preview deployment and redemption succeed. This is a recording plan, not evidence of a finished video. Keep a second unused invitation ready. Never display invitation secrets, organizer recovery content or wallet recovery phrases.

| Time    | Screen                                 | Narration                                                                                                               |
| ------- | -------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| 0–8 s   | Veil access screen                     | “Veil is private allowlist access on Midnight. Guests prove they belong without publishing their invitation.”           |
| 8–16 s  | Connected Lace on Preview              | “The wallet signs transactions. The invitation and membership witness are processed by my local proof server.”          |
| 16–25 s | Organizer event with sealed list       | “The organizer registers invitation commitments, then seals one fixed guest list.”                                      |
| 25–38 s | Guest imports file and requests access | “The Compact circuit checks a private Merkle path and derives an event-scoped nullifier.”                               |
| 38–48 s | Real confirmed receipt and block       | “Only after network confirmation does the app grant access. The secret and selected membership leaf are not published.” |
| 48–55 s | Repeat-use rejection                   | “The same invitation cannot be redeemed twice, even with a modified client.”                                            |
| 55–60 s | Passing tests/Actions and privacy view | “Tests cover forged witnesses, unauthorized writes and replay. CI compiles and tests every push.”                       |

Proof generation and confirmation may take longer than the video. Trim waiting time honestly with an explicit “waiting time shortened” label. Do not substitute local demo receipts for network confirmations. If showing the local demo as supplementary footage, keep its mode label visible.

Required final evidence: a playable video file or public link, actual confirmed transaction ID and the CI run URL at the recorded revision.
