# Implementation plan

The approved specification is in `superpowers/specs/2026-09-24-veil-design.md`.
The referenced writing-plans skill is not installed; this project-local plan records the implementation sequence.

1. Pin the official Preview-compatible Midnight versions. Install Compact locally in the ignored `.tools` directory.
2. Implement and compile a Merkle membership contract with private organizer authorization and one-use, event-bound nullifiers.
3. Exercise generated contract circuits with positive and adversarial runtime tests.
4. Build strict invitation serialization, memory-only witness state, real connector and provider integration.
5. Build the guest and organizer flows with actual ledger confirmation, error recovery and honest configuration states.
6. Apply the Veil identity: charcoal, graphite, warm white, muted lavender; Geist variable; geometric aperture logo; editorial split; nested surfaces and restrained motion.
7. Verify desktop/mobile, keyboard flow, accessibility, wallet rejection and repeat redemption.
8. Add compile/test/build CI and an opt-in GitHub Pages deployment job; prepare submission documentation and evidence.
9. Publish and verify external systems where authenticated access exists; explicitly record external prerequisites otherwise.

No live network outcome, hosted CI result, product approval or video evidence will be reported as complete without the corresponding evidence.
